# Install Temporal Go SDK

Initialize a Go project.

```bash
go mod init example.com/temporal-tutorial/L1
```

Install Temporal SDK with `go get`:

```bash
go get go.temporal.io/sdk
```

I don't like CLI. So I skipped here.
